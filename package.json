{
  "name": "d8pgroceries-agent",
  "version": "1.0.0",
  "description": "Scriptable iOS app: parse grocery lists, walk through Publix/Instacart searches",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/ tests/ scripts/",
    "bundle": "node scripts/bundle.js"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
